# Contributing

Keep all examples synthetic or fully anonymized. Add one fixture for each new
template or role profile, run the validation scripts, and avoid adding
dependencies unless they make the generated output more reliable.
