interface:
  display_name: "CV Builder"
  short_description: "Create tailored, ATS-safe resumes with visual QA."
  default_prompt: "Use $cv-builder to create a tailored, ATS-safe resume from my background and target job."
policy:
  allow_implicit_invocation: true
